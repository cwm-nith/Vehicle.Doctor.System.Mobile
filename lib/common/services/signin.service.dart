import 'package:get/get.dart';

class SigninService extends GetxService {
  static SigninService get to => Get.find();

  // Future<UserEntity> loginAsync(SigninRq rq) async {
  //   var res = await UserAPI.login(params: rq);
  //   return res;
  // }
}
